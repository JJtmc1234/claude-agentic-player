MISSION (from JJ) — FRESH MAP, competence-first. Build a CLEAN, AUTOMATED base that
actually WORKS, using the scripted build-macros (bridge/agent/build_macros.py), then
make it feel like a smart co-op teammate. Report milestones in chat (concise, no spam).

PHASE 1 — PROVE COMPETENCE (powered base, hands-off):
  1. Anchor the base at spawn, near water + ore (the fresh map should have both close).
  2. build_burner_mine on iron + copper + coal ore -> fueled drills into chests.
  3. build_smelt_from_chests -> fueled furnace columns -> iron + copper plates banking.
  4. build_steam_power at the water (pump -> boilers -> engines -> poles) -> live power grid.
  5. build_lab_bank -> place + power labs; feed red science packs -> research 'automation'.
     >>> MILESTONE: automation researching, end-to-end, no hand-holding.
  6. GREEN science next (logistic-science-pack), then keep climbing.

PHASE 2 — FEEL LIKE A TEAMMATE (after competence is proven):
  - PINGS = COMMANDS: JJ Alt-clicks a spot -> go there and help / build what he means.
  - CHAT REQUESTS: parse JJ's natural-language asks ("build green circuits here",
    "mine that copper", "wall the east") -> dispatch the right macro.
  - Talk like a teammate: brief, contextual acks + milestones + blockers. Never spam.
  - When idle: fix the worst bottleneck / prep the next tier -- but defer to JJ's lead.

RULES: CRAFT/PLACE real items (never spawn). Only build unlocked recipes. Use burner
inserters until power is up (electric ones sit no_power). Hope for help, plan for no help.
