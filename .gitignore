# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Local config / secrets — never commit these
.env
*.env
.env.*
!.env.example
bridge/local_config.json

# Claude Code local settings (user-specific permissions)
.claude/settings.local.json

# Editors
.vscode/
.idea/
*.swp

# OS junk
Thumbs.db
.DS_Store

# Logs and temp
*.log
tmp/
temp/

# Claude's deep-thought scratchpad (not a deliverable)
thoughts.txt

# Project BRAIN runtime state (regenerated at run time)
bridge/agent/team_status/
bridge/agent/requests/
bridge/agent/state/
bridge/agent/learned_actions.json
bridge/agent/base.json

# RL training artifacts — checkpoints and weight dumps. Generated by training.
checkpoints/
*.zip

# Factorio artifacts that might leak in by accident
*.zip
server-settings.json
start-server.bat
race.json
compete_standings.json
