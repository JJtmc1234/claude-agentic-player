# what-im-doing.txt — live status (READ THIS AT EVERY SESSION START / AFTER COMPACT)
# Updated: 2026-07-13. Paired with context.txt (stable), summary.txt (full history), CLAUDE.md.

>>> RESUME HERE (2026-07-13) — TEAM IS OFF. Killed a runaway; DO NOT relaunch until spawn-dedup fixed. <<<
FIREFIGHT (2026-07-13): the district went to "madness" — ~30 brain processes were alive as
python3.13.exe (NOT python.exe/pythonw.exe -> my process filters missed them all) plus a
background "relaunch brain" task, all autonomously spawning chars + carpeting belts. KILLED all
of them (match python* CommandLine ~ companion_brain|bridge.agent|launch_team, EXCLUDE $PID or the
kill cmd self-terminates -> exit 255). Verified STABLE: 1 char (JJ un=8), 225 belts, no growth.
CLEANUP DONE (JJ approved): destroyed only 0-item no-player chars (never any char with items);
consolidated ALL employee-char inventory into an IRON-CHEST at (177,-161) via direct inventory
move (chest cost 8 real iron-plate from a char, economy-honest). Belts KEPT per JJ. Chest holds:
coal120 copper-cable116 electronic-circuit40 iron-gear-wheel187 iron-plate170 iron-stick36
kr-automation-core12 wood48 +misc. Helper scripts: bridge/_district_scan.py, _destroy_empty_chars.py,
_consolidate.py, _growth.py, _who_builds.py (all read/one-shot; _flatten() strips Lua -- comments
before joining to one line — REQUIRED or the whole script gets commented out).
JJ (2026-07-13, done playing for the night): "just have the one" + "just fixing/coding for the
rest of the night" + "use your best judgment" (no more questions). So: single-companion mode,
4-employee team PARKED, and I spent the night hardening the code below. Game is DOWN now (RCON
refused) -> everything below is code-only, NOT tested in-game yet (verify when JJ next hosts).

FIXES DONE TONIGHT (all compile-clean; singleton mutex verified across 2 procs):
 1. Process scripts now match python3.13.exe: stop_team.ps1 + cleanup_python.ps1 use Name -like
    'python*' (was 'python.exe' only -> missed EVERY brain; that's why the runaway was unkillable).
    Both exclude $PID (a self-match self-terminates -> exit 255).
 2. Spawn-dedup (companion_brain.py): _char_valid() helper; _resolve_unum step1+step2 now VALIDATE
    the unum before reuse (list_chars hands back dead unums); _heal_companion no-ops if current
    _UNUM is still a valid character (don't respawn on a transient perceive glitch); hard per-proc
    spawn cap _SPAWN_COUNT/_MAX_SPAWNS=3.
 3. Singleton guard: _ensure_singleton() Windows named mutex (Global\claude_brain_<name>) in main()
    -> a 2nd BRAIN of the same name exits immediately. Structural cap of one-per-name. VERIFIED.
 4. "just have the one": bridge/agent/launch_one.ps1 (cleanup -All then launch ONE 'companion',
    no district). launch_team.ps1 PARKED (prints a notice + exit 0; 4-employee mode disabled).
 5. Anti-spam-building rule added to INTENT_SYSTEM: every placement needs a named purpose; belts
    only as short real source->dest runs, never a carpet; check-before-place; prefer idle over filler.
STILL TODO (durable, needs deploy+restart -> ASK JJ): make the mod's spawn_named_char idempotent
and have list_chars drop invalid unums (belt-and-suspenders for #2). Also: re-verify no belt-spam
with the live single companion; consider clearing the 225 district belts when the game is back
(JJ waffled keep->annoyed; my judgment = clear them next time in-game).
NOTE: game.get_entity_by_unit_number EXISTS (2.0.77); inv.get_contents() returns a LIST of
{name,count,quality} NOT a map -> sum st.count. RCON pw in C:\FactorioServer\start-server.bat.
Employee items are safe in an IRON-CHEST at (177,-161).

>>> (older resume note) waiting for JJ's "server's up" (he's restarting). <<<
RUN: 2.0 + Space Age + Krastorio 2 Spaced Out + Razi Protocol, NO enemies. CC 0.12.5 published.
STATE: burner-city STARTER only — NO real automated base. Assemblers only smelt plates;
everything past plates (K2 intermediates, tech cards, science) is HANDCRAFTED -> labs starve.
K2 recipes are non-vanilla, e.g. automation-science-pack = 1 kr-automation-core + 5
kr-blank-tech-card. ALWAYS query live prototypes.
EMPLOYEE DISTRICT (JJ built): a 97x97 sandbox at bbox (127,-225)..(224,-128), center ~(175,-176),
stone-path boundary, HAS POWER. Employees may build ANYTHING inside, REQUEST resources from
outside, but must NOT modify anything outside. "Have them plan + build it."
IN PROGRESS (companion_brain.py): multi-BRAIN team is parameterized (--name/--role, team_status
blackboard, launch_team.ps1 with roles miner/smelter/builder/engineer -> should become the
spec's E1-E4). Added DISTRICT config + _in_district() helper. NOT YET DONE:
  1. Gate place/build_mine/build_smelt/mine on _in_district (refuse building outside the sandbox).
  2. Add a set_recipe action (place assembler -> set its K2 recipe) so they can actually produce.
  3. A resource-REQUEST channel (employees ask for plates/items; manager delivers into a district
     chest) since they can't mine outside.
  4. Spawn the 4 employees INSIDE the district (--district "127,-225,224,-128"); give them the
     plan+build directive; launch; coordinate + verify (I am the MANAGER per JJ's ChatGPT spec —
     coordinate, don't be a 5th builder; no STATUS reports, just play).
NOTE: JJ pasted a full manager spec (K2SO 1.6.20 + Razi 1.6.7); key: I coordinate 4 employees,
they build in the sandbox, JJ owns layouts/major decisions, no reports (just play).

CURRENT PHASE: Project BRAIN — the JJ-CENTRIC intent loop. The companion's #1 job is JJ:
obey pings, understand + reply to chat, complement what JJ builds, and only advance the
base autonomously when JJ isn't directing. North star = reach the SOLAR SYSTEM EDGE (which
per JJ needs ALL 5 PLANETS COLONIZED first).

ACTIVE DRIVER: bridge/agent/companion_brain.py  (launch: `$env:ANTHROPIC_API_KEY =
[Environment]::GetEnvironmentVariable('ANTHROPIC_API_KEY','User'); python bridge/agent/companion_brain.py`)
  - Opus 4.8 "intent" tier decides {reply, action} each cycle (1s loop, no thinking -> fast
    + parseable). Executes via deterministic tools + build-macros.
  - Actions: goto, mine (rocks too: huge-rock gives coal), place/insert/take, craft, research,
    fetch (courier to JJ), equip + attack (combat), build_mine/build_smelt, plus PLAN
    (compose primitives) and remember/recall (learned_actions.json — invents+reuses actions).
  - Behaviors: talks only on JJ chat/ping/threat (no autonomous spam); explains reasoning;
    honest fetch (won't fake-deliver items that don't exist); newest chat message wins.
  - Robustness: self-heal (respawn on death), auto-reconnect + backoff, startup waits for
    the game/mod instead of crashing.
  - COMBAT: a script character does NOT auto-fire -> brain sets shooting_state=shooting_enemies
    every cycle when armed, so it torches biters in range (flamethrower works). Needs gun+AMMO.

BUILD MACROS: bridge/agent/build_macros.py — build_burner_mine + build_smelt_from_chests
  VALIDATED live; connect_belt + build_steam_power written but UNTESTED (finish live).

CURRENT STATE (2026-07-12): game up but claude-companion MOD NOT LOADED ("Unknown interface:
claude") -> brain (task) is WAITING; it auto-starts when JJ enables the mod. Fresh map has
trees + water at ~102 tiles; companion keeps dying when unarmed near biters (arm it).

GOTCHAS (see context.txt too): no furnace_source/result defines (crafter_input/output +
get_output_inventory/get_fuel_inventory); mod craft loop doesn't fire craft-item triggers;
burner inserters until power; first RCON command after game start is swallowed (warmup first);
mods load only at game start (can't enable via RCON).

NEXT: when the mod's on + JJ playing — tune the teammate feel live; finish steam-power macro;
climb toward power -> red science; keep arming/defending. Honesty > overclaim; commit on major.
