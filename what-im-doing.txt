# What I'm Doing — Live State

Updated on every substantive action. Paired with context.txt (stable). Sections:
Current / Done but uncommitted / Blocked / Next concrete steps.

## Current task

BC belt initial (16x16) — task b51z2hdud. 50 reps × 2000 epochs.
After: PPO belt from this BC ckpt with target_kl=0.005 (tighter than
cable's 0.01 since cable's PPO drifted into dead-chain territory).

## Cable 16x16 status (paused)

BC v4 (loss 0.19) + cable16_v3 PPO ran 17 eps. Best ep +160 with 24
cables in transit (8 asm + 1 ins + 15 belts). NEVER reached chest.
PPO drifted after ep 8: by ep 13-17 mostly dead chains (alive=False)
even with target_kl=0.01.

Saved as `maskppo_cable16_FINAL_BC_baseline.zip` (= cable16_v3_best.zip).

Lesson: in 16x16 PPO-from-BC needs target_kl=0.005 to avoid drift, but
even then learning is slow because the agent rarely places the right
belt at col 9-15 row 7 by random sampling. The mod 0.9.7 partial-credit
shaping pays for in-transit items so chains stay rewarding (chest=0,
asm=8 still gives per_gear=325) but doesn't pay enough to push past
the BC-equivalent ceiling.

Possible cable revisit later: deploy mod 0.9.8 with multi-machine
bonus + speed_bonus boost (staged in NEXT_DEPLOY.md), or design a
fuller partial demo (e.g. 9 actions including 2 more output belts).

## Solved milestones

- gears 8x8: BC v5b, 100% reached, 2 layout variants, +1660
- circuits 8x8: BC circ v1, 3/3 reached, +1785, 10 circuits

## Curriculum next

- belts 16x16 initial (CURRENT BC)
- circuits 16x16 initial (cables provided)
- mastery stages later — need 3rd input loader for some

## Solved milestones

- gears 8x8: BC v5b, 100% reached, 2 layout variants, +1660 reward
- circuits 8x8: BC circ v1, 3/3 reached, +1785 reward, 10 circuits
- gears 16x16 / cables 16x16: in flight via partial-demo approach

## Next when cable16_v3 produces chest output

1. Commit + tag cable16_v3 best ckpt
2. BC belt initial (iron-plate + iron-gear-wheel inputs)
3. BC circuit initial (iron-plate + copper-cable inputs)
4. Then mastery stages (compositional curriculum: agent makes intermediates)

## Done but uncommitted

- bridge/rcon_client.py: rewrote `command()` to use mirror-packet trick; default drain_timeout 1.0s → 0.05s.
- bridge/rl/callbacks.py (NEW): BestCheckpointCallback + EpisodeJsonlLogger.
- bridge/rl/grid_extractor.py (NEW): CNN features extractor for grid obs.
- bridge/rl/eval_checkpoint.py (NEW): deterministic eval over N episodes.
- bridge/rl/info_logger.py (NEW): env wrapper that writes per-episode info dict to JSONL.
- bridge/rl/plot_episodes.py (NEW): text + matplotlib summary of training jsonl.
- bridge/rl/post_train_report.py (NEW): one-shot report combining jsonl + eval + curve.
- bridge/rl/train_cables.py (NEW): cables curriculum entry, calls arena_set_task (needs mod 0.9.0).
- bridge/rl/train_masked.py: --policy mlp|cnn flag, callbacks wired in, save best + jsonl + info.
- bridge/_claude.py: lua_repr now handles dict and list.
- bridge/_check_speed.py, _check_dp.py, _patch_arena.py, _ping.py (probes).
- Mod 0.8.10 → 0.8.14 (display panels, chain bonuses, arena_set_config).
- progress-report.txt, context.txt, what-im-doing.txt at project root.
- ~11 new memory entries.

## Blocked

- eval_checkpoint.py needs RCON env — can't run while training holds it.
- Display-panel visual verification requires JJ in-game.
- Mod 0.9.0 (cables) blocked on chain_v2 producing a transferable checkpoint.

## Next concrete steps

1. Monitor chain_v2 progress (task b9mvbo286). Watch for early ep_rew_mean trends — if still negative after 5k steps, reward shaping needs another pass.
2. When training completes (or JJ stops it): run eval_checkpoint.py against `maskppo_chain_v2_best.zip` to quantify; render arena PNG of best episode for visual sanity check.
3. If chain_v2 converges (reached target ≥30% of episodes): apply circuit_changes.lua draft to mod, bump to 0.9.0, set up cables curriculum.
4. If chain_v2 plateaus before converging: tune reward shaping (likely raise activity_reward, reduce chain_pts weight if dominating) and re-run.
