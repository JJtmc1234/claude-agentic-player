# what-im-doing.txt — live status (READ THIS AT EVERY SESSION START / AFTER COMPACT)
# Updated: 2026-07-12. Paired with context.txt (stable), summary.txt (full history), CLAUDE.md.

CURRENT PHASE: Project BRAIN — the JJ-CENTRIC intent loop. The companion's #1 job is JJ:
obey pings, understand + reply to chat, complement what JJ builds, and only advance the
base autonomously when JJ isn't directing. North star = reach the SOLAR SYSTEM EDGE (which
per JJ needs ALL 5 PLANETS COLONIZED first).

ACTIVE DRIVER: bridge/agent/companion_brain.py  (launch: `$env:ANTHROPIC_API_KEY =
[Environment]::GetEnvironmentVariable('ANTHROPIC_API_KEY','User'); python bridge/agent/companion_brain.py`)
  - Opus 4.8 "intent" tier decides {reply, action} each cycle (1s loop, no thinking -> fast
    + parseable). Executes via deterministic tools + build-macros.
  - Actions: goto, mine (rocks too: huge-rock gives coal), place/insert/take, craft, research,
    fetch (courier to JJ), equip + attack (combat), build_mine/build_smelt, plus PLAN
    (compose primitives) and remember/recall (learned_actions.json — invents+reuses actions).
  - Behaviors: talks only on JJ chat/ping/threat (no autonomous spam); explains reasoning;
    honest fetch (won't fake-deliver items that don't exist); newest chat message wins.
  - Robustness: self-heal (respawn on death), auto-reconnect + backoff, startup waits for
    the game/mod instead of crashing.
  - COMBAT: a script character does NOT auto-fire -> brain sets shooting_state=shooting_enemies
    every cycle when armed, so it torches biters in range (flamethrower works). Needs gun+AMMO.

BUILD MACROS: bridge/agent/build_macros.py — build_burner_mine + build_smelt_from_chests
  VALIDATED live; connect_belt + build_steam_power written but UNTESTED (finish live).

CURRENT STATE (2026-07-12): game up but claude-companion MOD NOT LOADED ("Unknown interface:
claude") -> brain (task) is WAITING; it auto-starts when JJ enables the mod. Fresh map has
trees + water at ~102 tiles; companion keeps dying when unarmed near biters (arm it).

GOTCHAS (see context.txt too): no furnace_source/result defines (crafter_input/output +
get_output_inventory/get_fuel_inventory); mod craft loop doesn't fire craft-item triggers;
burner inserters until power; first RCON command after game start is swallowed (warmup first);
mods load only at game start (can't enable via RCON).

NEXT: when the mod's on + JJ playing — tune the teammate feel live; finish steam-power macro;
climb toward power -> red science; keep arming/defending. Honesty > overclaim; commit on major.
