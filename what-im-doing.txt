# what-im-doing.txt — live status (READ THIS AT EVERY SESSION START / AFTER COMPACT)
# Updated: 2026-07-11. Paired with context.txt (stable), summary.txt (full history), CLAUDE.md.

CURRENT PHASE: Project BRAIN v2 — replacing the reactive Haiku executor (which produced
non-functional spaghetti) with DETERMINISTIC SCRIPTED BUILD MACROS driven by a planner.
JJ's chosen direction (2026-07-11): relocate to water + scripted macros. Be autonomous,
don't ask (feedback-be-autonomous).

MACRO LIBRARY: bridge/agent/build_macros.py
  VALIDATED LIVE (worked on the iron square this session):
    - build_burner_mine(ore,count,cx,cy): burner drills on ore, fuels each, chest at each
      drill's real drop_position. Verified: 3 drills placed, all WORKING + mining into chests.
    - build_smelt_from_chests(cx,cy,ore): furnace column below each ore chest
      (chest->inserter->fueled furnace->inserter->output chest). Geometry VERIFIED EXACT.
      IMPORTANT: must use BURNER-inserters, not electric 'inserter' (no power -> electric
      sit no_power). Macro now uses burner-inserter + fuels them.
  WRITTEN BUT UNTESTED (verify next session):
    - connect_belt(x1,y1,x2,y2): L-path belt router.
    - build_steam_power(wx,wy): STUB — only places the offshore-pump; boiler->engine->pipe
      ->pole layout still to build+verify live (pump orientation is finicky).

KEY BLOCKERS:
  - MASTER BLOCKER = POWER, which needs WATER (nearest water ~90-122 tiles from the ore).
    Electric inserters/assemblers/labs all dead without it. Until then only BURNER
    mining+smelting works (drills, furnaces, burner-inserters).
  - JJ built HUGE ore squares ~200 tiles apart: iron@(130,-164) copper@(-125,-164)
    coal@(141,67) stone@(-183,47). Base = mining outposts + belt/rail haul to a core near water.
  - Companion low on drills + burner-inserters after this session; craft more (CRAFT DON'T SPAWN).

GAME CURRENTLY OFFLINE (JJ stopped; RCON refused). In-game work resumes when he hosts.

NEXT (when game is back):
  1. Finish build_steam_power (boiler/engine/pipe/pole) at water -> powered pole net.
  2. Pick base-core near water; compose macros: outposts -> belts -> smelt -> bus -> mall -> labs.
  3. Build the planner driver: Opus picks what/where -> calls deterministic macros (retire Haiku loop).
  4. Load 55 red packs into a POWERED lab -> 'automation' completes.
